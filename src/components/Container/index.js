import React from 'react';
import AddWorkout from '../AddWorkout/AddWorkout';

const Container = props => {
	// add isShown to state to be able to toggle
	return <React.Fragment></React.Fragment>;
};

export default Container;
